import {StyleSheet} from "react-native";

export default styles = StyleSheet.create({
	container: {
		position: "absolute",
		zIndex: -1,
		top: 0,
		right: 0,
		bottom: 0,
		left: 0,
		alignItems: "center",
		justifyContent: "center",
	},
	title: {
		fontSize: 15,
		letterSpacing: 0.8,
	}
});
