// @flow
import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  container: {
    marginLeft: 10,
  },
  icon: {
    width: 35,
    height: 35,
    resizeMode: 'contain',
  },
});
